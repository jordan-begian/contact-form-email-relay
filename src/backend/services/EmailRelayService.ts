import { ContactFormFields } from '@/shared/models/ContactFormFields';

const service = {
  emailRelay: async (form: ContactFormFields) => {
    // TODO: Add email relay logic to send form data via nodemailer util
    return;
  }
};

export default service;
